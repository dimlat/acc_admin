import 'package:flutter/material.dart';

double get kDefaultPadding => 9;

kHeight({int times = 1}) => SizedBox(height: kDefaultPadding * times);
kWidht({int times = 1}) => SizedBox(width: kDefaultPadding * times);
