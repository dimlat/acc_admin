import 'package:flutter/material.dart';

class ContentMenu extends StatelessWidget {
  const ContentMenu({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("here is the content will shown"));
  }
}